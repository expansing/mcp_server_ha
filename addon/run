#!/bin/bash

set -e

export HA_URL="${HA_URL:-http://homeassistant:8123}"
export HA_TOKEN="${HA_TOKEN:-}"
export HA_VERIFY_SSL="${HA_VERIFY_SSL:-false}"

if [ -z "$HA_TOKEN" ]; then
    echo "Error: HA_TOKEN is required"
    exit 1
fi

/app/venv/bin/python -m ha_mcp.server
